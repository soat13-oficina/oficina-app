spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/oficina_db
    username: oficina_user
    password: oficina_123
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
